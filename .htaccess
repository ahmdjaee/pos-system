<IfModule mod_rewrite.c>
    RewriteEngine On

    # Shared hosting usually points the domain to the project root.
    # Forward every request to Laravel's public directory.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
